interface:
  display_name: "GameDev Production Pipeline"
  short_description: "Preflight, one owner, parallel audits, review, and QA"
  default_prompt: "Use $gamedev-pipeline to run the approved feature through director preflight, one persistent writing owner, parallel read-only convergence, risk-based Review closure, support/evidence recovery, runtime QA, and production-readiness within the configured worker budget."
