interface:
  display_name: "GameDev Production Pipeline"
  short_description: "Explicit-only bounded GameDev production pipeline"
  default_prompt: "Use $gamedev-pipeline as the sole v2 Director for exact requirements/specification/plan authority and structured slices; delegate fresh phase workers and continue through verified production-ready candidate."
policy:
  allow_implicit_invocation: false
