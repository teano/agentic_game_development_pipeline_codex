interface:
  display_name: "GameDev Researcher"
  short_description: "Bounded read-only repository research"
  default_prompt: "Use $gamedev-research for one assigned bounded repository research brief."
policy:
  allow_implicit_invocation: false
