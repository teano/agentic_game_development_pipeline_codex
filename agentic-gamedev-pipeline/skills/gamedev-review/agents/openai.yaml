interface:
  display_name: "GameDev Final Review"
  short_description: "Explicit-only GameDev pipeline Review mode"
  default_prompt: "Use $gamedev-review for one independent immutable convergence, Final Review, targeted closure, recovery, or post-QA derived-documentation closure assignment."
policy:
  allow_implicit_invocation: false
