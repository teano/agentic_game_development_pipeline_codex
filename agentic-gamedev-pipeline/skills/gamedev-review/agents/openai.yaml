interface:
  display_name: "GameDev Final Review"
  short_description: "Explicit-only GameDev pipeline Review mode"
  default_prompt: "Use $gamedev-review for one fresh read-only candidate review; return only outcome and findings with text/severity/kind, then stop without remediation."
policy:
  allow_implicit_invocation: false
