interface:
  display_name: "GameDev Final Review"
  short_description: "Explicit-only GameDev pipeline Review mode"
  default_prompt: "Use $gamedev-review to perform the assigned read-only convergence lens, full final Review, targeted local-product closure, or support/evidence recovery verification without editing the product."
policy:
  allow_implicit_invocation: false
