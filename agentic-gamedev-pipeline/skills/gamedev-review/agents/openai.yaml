interface:
  display_name: "GameDev Final Review"
  short_description: "Explicit-only GameDev pipeline Review mode"
  default_prompt: "Use $gamedev-review for one immutable capsule-bounded review assignment with exact component credit, then return NEXT_ACTION and stop."
policy:
  allow_implicit_invocation: false
