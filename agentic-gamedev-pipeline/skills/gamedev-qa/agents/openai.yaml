interface:
  display_name: "GameDev Runtime QA"
  short_description: "Explicit-only GameDev runtime QA mode"
  default_prompt: "Use $gamedev-qa to complete every independent runtime scenario for one reviewed feature, preserve exact evidence, and classify product failures separately from execution gates."
policy:
  allow_implicit_invocation: false
