interface:
  display_name: "GameDev Runtime QA"
  short_description: "Explicit-only GameDev runtime QA mode"
  default_prompt: "Use $gamedev-qa to execute the finalized exact manual identity matrix on the immutably reviewed product/evidence revision and classify product failures separately from execution gates."
policy:
  allow_implicit_invocation: false
