interface:
  display_name: "GameDev Runtime QA"
  short_description: "Explicit-only GameDev runtime QA mode"
  default_prompt: "Use $gamedev-qa to execute every exact manual identity, persist the controller-compatible aggregate, return NEXT_ACTION, and stop."
policy:
  allow_implicit_invocation: false
