interface:
  display_name: "GameDev Runtime QA"
  short_description: "Explicit-only GameDev runtime QA mode"
  default_prompt: "Use $gamedev-qa for one fresh read-only candidate QA assignment; return only outcome, checks, and blocker when blocked, then stop without remediation."
policy:
  allow_implicit_invocation: false
