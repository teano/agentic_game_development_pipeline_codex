interface:
  display_name: "GameDev Runtime QA"
  short_description: "Explicit-only GameDev runtime QA mode"
  default_prompt: "Use $gamedev-qa to execute every exact manual identity, return the schema-2 execution envelope and evidence for controller aggregation plus NEXT_ACTION, and stop."
policy:
  allow_implicit_invocation: false
