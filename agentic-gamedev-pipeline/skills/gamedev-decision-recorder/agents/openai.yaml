interface:
  display_name: "GameDev Decision Recorder"
  short_description: "Record accepted decisions without inventing them"
  default_prompt: "Use $gamedev-decision-recorder to record one receipt-backed accepted decision pass, return its completion token and NEXT_ACTION, and stop."
policy:
  allow_implicit_invocation: false
