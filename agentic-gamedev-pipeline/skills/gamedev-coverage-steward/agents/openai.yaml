interface:
  display_name: "GameDev Coverage Steward"
  short_description: "Plan and finalize exact feature coverage"
  default_prompt: "Use $gamedev-coverage-steward for one exact planning or finalization assignment, persist the coverage artifact, return NEXT_ACTION, and stop."
policy:
  allow_implicit_invocation: false
