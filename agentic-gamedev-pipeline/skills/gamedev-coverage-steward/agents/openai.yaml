interface:
  display_name: "GameDev Coverage Advisory"
  short_description: "Advisory review of exact feature coverage"
  default_prompt: "Use $gamedev-coverage-steward only for a standalone advisory review of supplied coverage data; return COVERAGE_COMPLETE plus NEXT_ACTION, and stop."
policy:
  allow_implicit_invocation: false
