interface:
  display_name: "GameDev Engineer"
  short_description: "Explicit-only bounded GameDev engineering pass"
  default_prompt: "Use $gamedev-engineer for one exclusive production implementation or root-cause remediation pass with tightly coupled automated tests, targeted checks, final diff inspection, and a short semantic handoff."
policy:
  allow_implicit_invocation: false
