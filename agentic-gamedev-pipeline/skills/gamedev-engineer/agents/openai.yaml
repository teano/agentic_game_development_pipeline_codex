interface:
  display_name: "GameDev Engineer"
  short_description: "Explicit-only bounded GameDev engineering pass"
  default_prompt: "Use $gamedev-engineer for one controller-assigned current-slice implementation or Review/QA remediation; return only the compact engineering semantic artifact and stop."
policy:
  allow_implicit_invocation: false
