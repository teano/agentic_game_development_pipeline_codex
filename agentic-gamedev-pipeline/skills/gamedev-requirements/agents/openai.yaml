interface:
  display_name: "GameDev Requirements"
  short_description: "Explicit-only GameDev requirements workflow"
  default_prompt: "Use $gamedev-requirements to produce the canonical approved PRD, return PRD_READY plus NEXT_ACTION, and stop without starting specification."
policy:
  allow_implicit_invocation: false
